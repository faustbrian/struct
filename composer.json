{
    "name": "cline/struct",
    "description": "Lean, attribute-driven Laravel data objects with explicit validation and Eloquent casting support",
    "license": "MIT",
    "keywords": [
        "php"
    ],
    "authors": [
        {
            "name": "Brian Faust",
            "email": "brian@cline.sh"
        }
    ],
    "require": {
        "php": "^8.5.0",
        "cline/attribute-reader": "^1.0",
        "facade/ignition-contracts": "^1.0.2",
        "laravel/framework": "^10.0 || ^11.0 || ^12.0 || ^13.0",
        "spatie/laravel-package-tools": "^1.93.0",
        "spatie/php-structure-discoverer": "^2.4",
        "symfony/yaml": "^7.4.6"
    },
    "require-dev": {
        "beacon-hq/bag": "^2.6.2",
        "cline/bench": "^1.0.1",
        "cline/coding-standard": "^2.0.1",
        "cline/money": "^2.0",
        "cline/numerus": "^5.0",
        "livewire/livewire": "^4.2.1",
        "orchestra/testbench": "^8.0 || ^9.0 || ^10.9",
        "spatie/laravel-data": "^4.20",
        "symfony/var-dumper": "^6.0 || ^7.4.6"
    },
    "suggest": {
        "cline/money": "Enable built-in Money casting and the #[AsMoney(...)] attribute for currency-aware DTO fields.",
        "cline/numerus": "Enable built-in Numerus casting and numeric normalization attributes such as #[Round], #[Clamp], and #[Abs].",
        "livewire/livewire": "Add first-class Livewire Wireable and synthesizer support for data objects."
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "Cline\\Struct\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Benchmark\\": "benchmark/",
            "Tests\\": "tests/"
        }
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "ergebnis/composer-normalize": true,
            "pestphp/pest-plugin": true
        }
    },
    "extra": {
        "laravel": {
            "aliases": {
                "Struct": "Cline\\Struct\\Facades\\Struct"
            },
            "providers": [
                "Cline\\Struct\\StructServiceProvider"
            ]
        }
    },
    "scripts": {
        "bench": "php bin/bench run",
        "bench:compare": "php bin/bench compare --against=snapshot:latest",
        "bench:save": "php bin/bench snapshot:save baseline",
        "lint": "vendor/bin/ecs check --fix",
        "refactor": "rector",
        "test": [
            "@test:lint",
            "@test:type-coverage",
            "@test:unit",
            "@test:types",
            "@test:refactor"
        ],
        "test:lint": "vendor/bin/ecs check",
        "test:refactor": "rector --dry-run",
        "test:type-coverage": "pest --parallel --min=80 --type-coverage",
        "test:types": "phpstan",
        "test:unit": "pest --parallel --min=80 --coverage"
    }
}
